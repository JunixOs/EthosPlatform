Comandos de pruebas (desde raíz)
Comando	Qué hace
pnpm test:backend	Ejecuta los 92 tests unitarios del backend
pnpm test:frontend	Ejecuta los 38 tests unitarios del frontend
pnpm test	Ejecuta tests de ambos proyectos (backend + frontend)
pnpm test:backend:watch	Modo watch del backend (re-ejecuta al guardar)
pnpm test:frontend:watch	Modo watch del frontend (re-ejecuta al guardar)
pnpm test:backend:coverage	Tests + reporte de cobertura del backend
pnpm test:frontend:coverage	Tests + reporte de cobertura del frontend
pnpm test:coverage	Cobertura de ambos proyectos